<p id="notice"><%= notice %></p>

<h1>Aktuelles</h1>
<!--
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Subtitle</th>
      <th>Story</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody> -->
  
  <% @color = true %>
    <% @newssystems.each do |newssystem|
        if(@color == true)
          @color = false;%>

      <div class="thumbnail col-xs-12 col-sm-6 col-md-3" style="">
        <div class="caption">
          <h3><%= newssystem.created_at.strftime("%d.%m.%Y") %><br><%= newssystem.title %></h3>
          <p><%= newssystem.story %></p>
          <%= link_to 'weiterlesen', newssystem, :style => 'float: right;color: #f4bfa5;'%>

        </div>
      </div>
        <% elsif(@color == false)
          @color = true%>
        <div class="thumbnail col-xs-12 col-sm-6 col-md-3" style="background-color: #dfdfde;">
          <div class="caption">
            <h3><%= newssystem.created_at.strftime("%d.%m.%Y") %><br><%= newssystem.title %></h3>
            <p><%= newssystem.story %></p>
            <%= link_to 'weiterlesen', newssystem, :style => 'float: right;color: #f4bfa5;'%>

          </div>
        </div>
        <% end %>
      
        <!--
      <tr>
        <td><%#= newssystem.title %></td>
        <td><%#= newssystem.subTitle %></td>
        <td><%#= newssystem.story %></td>
        <td><%#= link_to 'Show', newssystem %></td>
        <td><%#= link_to 'Edit', edit_newssystem_path(newssystem) %></td>
        <td><%#= link_to 'Destroy', newssystem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>-->
    <% end %>
 <!-- </tbody>
</table> -->

<br>
<% if current_user.present? %>
<div class="col-sm-12">
<%= link_to 'Neue News erstellen', new_newssystem_path %>
</div>
<% end %>
