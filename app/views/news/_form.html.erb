<%= form_for(news) do |f| %>
  <% if news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
      <% news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :subTitle %>
    <%= f.text_field :subTitle %>
  </div>

  <div class="field">
    <%= f.label :story %>
    <%= f.text_area :story %>
  </div>

  <div class="field">
    <%= f.label :sendDate %>
    <%= f.datetime_select :sendDate %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
