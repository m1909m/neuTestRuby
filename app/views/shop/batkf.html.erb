<div id="message" class="alert alert-success fade in" style="display: none;"><button id="bClose" class="close" >x</button>Sie haben erfolgreich Artikel in den Warenkorb hinzugefügt.</div>
<div class="col-xs-12">
  <h1>Shop</h1>
</div>
<div class="col-xs-12" style="margin-bottom: 1em;">
  <%= image_tag("Titelbild_BAT-KF.jpg", :alt => "BAT-KF", :class => "img-responsive center-block") %>

</div>
<%= @page.content.html_safe %>

<div class="col-xs-12 col-sm-6">
  <div ng-controller="shopController" >
    <table class="table table-responsive" data-ng-init="init()">
      <thead>
        <tr>
          <th>Artikel</th>
          <th>Preis</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <tr ng-repeat="article in articles">
          <td>{{article.title}}</td>
          <td>{{article.price | currency: '€'}}</td>
          <td><input type="number" min="0" ng-model="article.anzahl" /></td>
        </tr>
        <tr>
          <td></td>
          <td></td>
          <td><a href class="btn btn-default btn-sm" ng-click="addArticle(articles)">In den Warenkorb</a></td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
<script type="text/javascript">
    $(document).ready(function() {
        $("#bClose").click(function () {
        //    if ($("#message").is(":visible")) {
        //        $("#message").hide();
            //} else {
                $("#message").show();
        //    }
        });
    });

</script>


