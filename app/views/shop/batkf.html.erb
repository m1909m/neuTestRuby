<div id="message" class="alert alert-success fade in" style="display: none">
  <a class="close" onclick="$('.alert').hide()">×</a>Ihre Artikel wurden erfolgreich in den Warenkorb gelegt.</div>
<div class="col-xs-12">
  <h1>BAT-KF</h1>
</div>
<div class="col-xs-12" style="margin-bottom: 1em;">
  <%= image_tag("Titelbild_BAT-KF.jpg", :alt => "BAT-KF", :class => "img-responsive center-block") %>

</div>
<%= @page.content.html_safe %>

<div class="col-xs-12 col-sm-6">
  <div ng-controller="shopController" class="table-scrollable">
    <table class="table table-responsive" data-ng-init="init()">
      <thead>
        <tr>
          <th>Artikel</th>
          <th>Preis</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <tr ng-repeat="article in articles">
          <td>{{article.title}}</td>
          <td>{{article.price | currency: '€'}}</td>
          <td><input type="number" min="0" ng-model="article.anzahl" /></td>
        </tr>
        <tr>
          <td></td>
          <td></td>
          <td><a href class="btn btn-default btn-sm" ng-click="addArticle(articles)">In den Warenkorb</a></td>
          <td><a href="/warenkorb" target="_self" class="btn btn-default btn-sm" ng-show="cart.length">Zum Warenkorb</a></td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
<script type="text/javascript">

</script>

