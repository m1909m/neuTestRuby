<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Ihre Buchung</title>
</head>
<body>
<p>Vielen Dank für Ihre Bestellung<br />

Sie haben folgende Artikel bestellt:</p>
<%
  @articles.each do |a|
    if a['anzahl'] > 1
      @anzahl = true
    end

  end
%>
<table>
  <thead>
  <td style="width: 250px;">Artikel</td>
  <td>Preis</td>
  <% if @anzahl %>

    <td>Anzahl</td>
  <% else %>
    <td></td>
  <% end %>
  </thead>
  <tbody>
  <% @articles.each do |a| %>
      <% @sum = @sum + a['sum'] %>
      <tr>
        <td style="width: 250px;"><%= a['title'] %></td>
        <td style="width: 50px;"><%= a['price'] %>0 €</td>
        <% if a['anzahl'] > 1 %>

          <td><%= a['anzahl'] %></td>
        <% end %>
        <td></td>
      </tr>
  <% end %>
  <tr>
    <td style="text-align: right; width: 250px;">Summe:</td>
    <td style="width: 50px;"><%= @sum %>0 €</td>
    <td>inkl. MwSt., zzgl.  Porto und Versandkosten</td>
  </tr>
  </tbody>
</table>
  <p>Hinweise:<br />Die Rechnung erhalten Sie zusammen mit der Ware.</p>
<p>Bitte beachten Sie, dass je nach Bestellmenge noch entsprechende Versandkosten berechnet werden.<br />Bei Rückfragen dazu, wenden Sie sich bitte an unsere Geschäftsstelle.</p>




<p>Mit freundlichen Grüßen</p>
<p>Ihr vkm-rwl</p>
</body>
</html>