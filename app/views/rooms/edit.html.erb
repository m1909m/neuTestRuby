<div class="col-xs-12">
  <h1>Veranstaltung bearbeiten</h1>
</div>
<div class="col-xs-12">
  <form class="new_event" id="new_event" action="/administrator/rooms/event/<%= @id %>" accept-charset="UTF-8" method="post">
  <input name="utf8" type="hidden" value="&#x2713;" /><input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />


    <div class="col-xs-12">
      <div class="form-group">
        <label for="title">Titel</label>
        <input id="title" type="text" name="c_event[title]" class="form-control" placeholder="Titel" value="<%= @c_event.title %>" required/>
      </div>
    </div>
    <div class="col-xs-12">
      <div class="form-group">
        <label for="description">Beschreibung</label>
        <textarea id="description" type="text" name="c_event[description]" class="form-control" placeholder="Beschreibung" ><%= @c_event.description %></textarea>
      </div>
    </div>
    <div class="col-xs-12">
      <label>Tag 1</label>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker1">Startdatum</label>
          <div class='input-group date' id='datetimepicker1'>
            <input type='text' class="form-control" name="c_event[start]" value="<%= @c_event.start.to_time.strftime("%d.%m.%Y %k:%M") %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker2">Enddatum</label>
          <div class='input-group date' id='datetimepicker2'>
            <input type='text' class="form-control" name="c_event[end]" value="<%= @c_event.end.to_time.strftime("%d.%m.%Y %k:%M") %>'" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
    </div>
    <div class="col-xs-12">
      <!--    <div class="form-group">
              <label for="date">Zeitraum</label>
              <input date-range-picker id="date" class="form-control date-picker" type="text" ng-model="datePicker.date" options='{locale: { applyLabel: "Waehlen", fromLabel: "Von", format: "DD-MM-YYYY HH:mm", toLabel: "Bis", cancelLabel: "Schließen" }, timePicker: true, timePickerIncrement: 5, timePicker24Hour: true, startDate: Date.today, endDate: Date.today}'/>
          </div> -->
      <label>Tag 2</label>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker3">Startdatum</label>
          <div class='input-group date' id='datetimepicker3'>
            <input type='text' class="form-control" name="c_event[startSecond]" value="<%= @c_event.startSecond != "" && @c_event.startSecond != nil ? @c_event.startSecond.to_time.strftime("%d.%m.%Y %k:%M") : "" %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker4">Enddatum</label>
          <div class='input-group date' id='datetimepicker4'>
            <input type='text' class="form-control" name="c_event[endSecond]" value="<%= @c_event.endSecond != "" && @c_event.endSecond != nil  ? @c_event.endSecond.to_time.strftime("%d.%m.%Y %k:%M") : "" %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
    </div>
    <div class="col-xs-12">
      <!--    <div class="form-group">
              <label for="date">Zeitraum</label>
              <input date-range-picker id="date" class="form-control date-picker" type="text" ng-model="datePicker.date" options='{locale: { applyLabel: "Waehlen", fromLabel: "Von", format: "DD-MM-YYYY HH:mm", toLabel: "Bis", cancelLabel: "Schließen" }, timePicker: true, timePickerIncrement: 5, timePicker24Hour: true, startDate: Date.today, endDate: Date.today}'/>
          </div> -->
      <label>Tag 3</label>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker5">Startdatum</label>
          <div class='input-group date' id='datetimepicker5'>
            <input type='text' class="form-control" name="c_event[startThird]" value="<%= @c_event.startThird != "" && @c_event.startThird != nil  ? @c_event.startThird.to_time.strftime("%d.%m.%Y %k:%M") : "" %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker6">Enddatum</label>
          <div class='input-group date' id='datetimepicker6'>
            <input type='text' class="form-control" name="c_event[endThird]" value="<%= @c_event.endThird != "" && @c_event.endThird != nil  ? @c_event.endThird.to_time.strftime("%d.%m.%Y %k:%M") : "" %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
    </div>
    <div class="col-xs-12">
      <div class="form-group">
        <label for="minSize">Mindestanzahl an Mitgliedern</label>
        <input id="minSize" type="number" min="0" name="c_event[minSize]" class="form-control" placeholder="Mindestanzahl an Mitgliedern" value="<%= @c_event.minSize %>" />
      </div>
    </div>
    <div class="col-xs-12">
      <div class="form-group">
        <label for="maxSize">Maximalanzahl an Mitgliedern</label>
        <input id="maxSize" type="number" min="0" name="c_event[maxSize]" class="form-control" placeholder="Maximalanzahl an Mitgliedern" value="<%= @c_event.maxSize %>" />
      </div>
    </div><!--
<div class="col-xs-12">

        <div class="form-group">
            <label for="dateL">Anmeldezeitraum</label>
            <input date-range-picker id="dateL" class="form-control date-picker" type="text" name="c_event[dateL]" options='{locale: { applyLabel: "Waehlen", fromLabel: "Von", format: "DD.MM.YYYY HH:mm", toLabel: "Bis", cancelLabel: "Schließen" }, timePicker: true, timePickerIncrement: 5, timePicker24Hour: true, startDate: Date.today, endDate: Date.today}'/>
        </div>
</div>  -->
    <div class="col-xs-12">
      <!--    <div class="form-group">
              <label for="date">Zeitraum</label>
              <input date-range-picker id="date" class="form-control date-picker" type="text" ng-model="datePicker.date" options='{locale: { applyLabel: "Waehlen", fromLabel: "Von", format: "DD-MM-YYYY HH:mm", toLabel: "Bis", cancelLabel: "Schließen" }, timePicker: true, timePickerIncrement: 5, timePicker24Hour: true, startDate: Date.today, endDate: Date.today}'/>
          </div> -->
      <label>Anmeldezeitraum</label>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker5">Startdatum</label>
          <div class='input-group date' id='datetimepicker7'>
            <input type='text' class="form-control" name="c_event[startLogin]" value="<%= @c_event.startLogin.to_time.strftime("%d.%m.%Y %k:%M") %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
      <div class='col-sm-5'>
        <div class="form-group">
          <label for="datetimepicker6">Enddatum</label>
          <div class='input-group date' id='datetimepicker8'>
            <input type='text' class="form-control" name="c_event[endLogin]" value="<%= @c_event.endLogin.to_time.strftime("%d.%m.%Y %k:%M") %>" />
            <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
          </div>
        </div>
      </div>
    </div>

    <div class="form-group">
      <button type="submit" class="btn btn-default" >Änderungen Speichern</button>
    </div>
  </form>
  <a data-confirm="Sind Sie sich sicher?" class="btn btn-danger"  style="margin-top: 20px;" rel="nofollow" data-method="delete" href="/event/<%= @c_event.id %>">Veranstaltung Löschen</a>
</div>
<div class="col-sm-6">
  <h2>Dokument hochladen</h2>
  <form class="new_doc" id="new_doc" enctype="multipart/form-data" action="/administrator/rooms/event/<%= @id %>/doc" accept-charset="UTF-8" method="post">
    <input name="utf8" value="✓" type="hidden"><input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
    <div class="form-group">
      <label class="control-label" for="doc_name">Dokumentenname</label>
      <div class="" style="padding-left: 0px;padding-right: 0px;">
        <input class="form-control" name="doc[name]" id="doc_name" type="text">
      </div>
    </div>
    <div class="form-group">
      <label class="control-label" for="doc_attachment">Quelle</label>
      <div class="input-group">
        <label class="input-group-btn">
                    <span class="btn btn-default">
                        Durchsuchen<input type="file" style="display: none;" name="doc[attachment]" id="doc_attachment" multiple>
                    </span>
        </label>
        <input type="text" class="form-control" readonly>
     <!--   <input class="form-control" name="doc[attachment]" id="doc_attachment" type="file"> -->
      </div>
    </div>
    <div class="form-group">
      <input class="btn btn-default" name="commit" value="Hochladen" class="btn btn-default" data-disable-with="Hochladen" type="submit">
    </div>
  </form>
</div>
<div class="col-sm-6">
  <h2>Dokument auswählen</h2>
  <form class="new_docExist" id="new_docExist" enctype="multipart/form-data" action="/administrator/rooms/event/<%= @id %>/docExist" accept-charset="UTF-8" method="post">
    <input name="utf8" value="✓" type="hidden"><input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
    <div class="form-group">
      <label class="control-label" for="docExist_name">Dokumentenname</label>
      <select class="form-group form-control" name="docExist[resume_id]" id="docExist_name" >
        <% @allDocuments.each do |resume| %>
            <option value="<%= resume.id %>" data-othervalue="<%= resume.attachment_url %>"><%= resume.name %></option>
        <% end %>
      </select>
    </div>
    <div class="form-group">
      <input class="btn btn-default" name="commit" value="Auswählen" class="btn btn-default" data-disable-with="Auswählen" type="submit">
    </div>
  </form>
  <div class="col-sm-12">
    <a id="linkDocument" style="color: #f59d0e;" href="" target="_blank" rel="noopener noreferrer">Anzeigen</a>
  </div>
</div>
<div class="col-sm-12">
  <h2>Dokumente der Veranstaltung</h2>
  <% if @documents.length > 0 %>
  <table class="table table-responsive table-striped">
    <thead>.
    <tr>
      <th>Dokumentenname</th>
      <th></th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <% @documents.each do |resume| %>

        <tr>
          <td><%= resume[:name] %></td>
          <td><a style="color: #f59d0e;" href="<%= resume[:url] %>" target="_blank" rel="noopener noreferrer">Anzeigen</a></td>
          <td>
            <a data-confirm="Sind Sie sich sicher?" class="btn btn-danger" rel="nofollow" data-method="delete" href="/account/<%= resume[:accountId] %>">Dokument entfernen</a>

          </td>
        </tr>

    <% end %>
    </tbody>

  </table>
  <% else%>
    <p>Die Veranstaltung hat keine Dokumente</p>
  <% end %>



</div>

<script type="text/javascript">
    $(document).on('change', ':file', function() {
        var input = $(this),
            numFiles = input.get(0).files ? input.get(0).files.length : 1,
            label = input.val().replace(/\\/g, '/').replace(/.*\//, '');
        input.trigger('fileselect', [numFiles, label]);
    });
    $(document).ready(function(){
        $('#docExist_name').on('change', function() {
          var otherValue = $('#docExist_name').attr('data-othervalue');
          $('#linkDocument').attr('href', $('#docExist_name').attr('data-othervalue'));
          $('#linkDocument').text($('#docExist_name').value + ' Anzeigen');

        });
        $(':file').on('fileselect', function(event, numFiles, label) {

            var input = $(this).parents('.input-group').find(':text'),
                log = numFiles > 1 ? numFiles + ' files selected' : label;

            if( input.length ) {
                input.val(log);
            } else {
                if( log ) alert(log);
            }

        });


        $('#datetimepicker1').datetimepicker({locale: 'de'});
        $('#datetimepicker2').datetimepicker({
            locale: 'de',
            useCurrent: false //Important! See issue #1075
        });
        $("#datetimepicker1").on("dp.change", function (e) {
            //          $scope.datePicker.startOne = $("#datetimepicker1").val();
            $('#datetimepicker2').data("DateTimePicker").minDate(e.date);
        });
        $("#datetimepicker2").on("dp.change", function (e) {
            //          $scope.datePicker.endOne = $("#datetimepicker2").val();
            $('#datetimepicker1').data("DateTimePicker").maxDate(e.date);
        });

        $('#datetimepicker3').datetimepicker({locale: 'de'});
        $('#datetimepicker4').datetimepicker({
            locale: 'de',
            useCurrent: false //Important! See issue #1075
        });
        $("#datetimepicker3").on("dp.change", function (e) {
            //          $scope.datePicker.startTwo = $("#datetimepicker3").val();
            $('#datetimepicker4').data("DateTimePicker").minDate(e.date);
        });
        $("#datetimepicker4").on("dp.change", function (e) {
            //          $scope.datePicker.endTwo = $("#datetimepicker4").val();
            $('#datetimepicker3').data("DateTimePicker").maxDate(e.date);
        });

        $('#datetimepicker5').datetimepicker({locale: 'de'});
        $('#datetimepicker6').datetimepicker({
            locale: 'de',
            useCurrent: false //Important! See issue #1075
        });
        $("#datetimepicker5").on("dp.change", function (e) {
            //        $scope.datePicker.startThree = $("#datetimepicker5").val();
            $('#datetimepicker6').data("DateTimePicker").minDate(e.date);
        });
        $("#datetimepicker6").on("dp.change", function (e) {
            //  $scope.datePicker.endThree = $("#datetimepicker6").val();
            $('#datetimepicker5').data("DateTimePicker").maxDate(e.date);
        });
        $('#datetimepicker7').datetimepicker({locale: 'de'});
        $('#datetimepicker8').datetimepicker({
            locale: 'de',
            useCurrent: false //Important! See issue #1075
        });
        $("#datetimepicker7").on("dp.change", function (e) {
            //        $scope.datePicker.startThree = $("#datetimepicker5").val();
            $('#datetimepicker8').data("DateTimePicker").minDate(e.date);
        });
        $("#datetimepicker8").on("dp.change", function (e) {
            //  $scope.datePicker.endThree = $("#datetimepicker6").val();
            $('#datetimepicker7').data("DateTimePicker").maxDate(e.date);
        });
    });
</script>

























